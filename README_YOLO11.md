# Инструкция по запуску робота следящего за котом

## Готовые файлы

После реализации у вас есть следующие файлы:

```
ESP32_car_cat/
├── config.py                              # Конфигурация всех параметров
├── cat_follower_yolo11.py                 # Основной скрипт с YOLO11
├── test_detection.py                      # Тест детекции без управления моторами
├── esp32_cat_follower_simple/
│   └── esp32_cat_follower_simple.ino      # Обновлённый Arduino скетч (поворот 500мс)
└── files/
    └── cat_follower_robot.py              # Старая версия (для справки)
```

---

## Шаг 1: Обновить ESP32

### 1.1 Загрузить новый скетч

1. Откройте Arduino IDE
2. Откройте файл `esp32_cat_follower_simple/esp32_cat_follower_simple.ino`
3. Нажмите "Загрузить" (Upload)
4. Дождитесь успешной загрузки

### 1.2 Проверить работу

1. Откройте "Монитор порта" (Serial Monitor) на скорости 115200
2. Вы должны увидеть:
   ```
   CONNECTED!
   IP address: 192.168.0.112
   ```
3. Запишите этот IP адрес (он понадобится для config.py)

### 1.3 Проверить время поворота

Откройте браузер и проверьте:
- `http://192.168.0.112/left` → должен повернуть 0.5 сек
- `http://192.168.0.112/right` → должен повернуть 0.5 сек

✅ ESP32 готов!

---

## Шаг 2: Настроить Python окружение

### 2.1 Установить зависимости

Откройте терминал (Bash в VS Code) в папке проекта:

```bash
# Создать виртуальное окружение (если ещё не создано)
python -m venv cat_env

# Активировать окружение (Bash)
source cat_env/Scripts/activate

# Установить библиотеки
pip install ultralytics opencv-python requests numpy

# Для Intel процессоров - установить OpenVINO (опционально, но рекомендуется)
pip install openvino
```

**Важно**:
- YOLO11 устанавливается через библиотеку `ultralytics`
- OpenVINO ускорит модель на Intel процессорах (i5-1235U) в 1.5-3 раза

### 2.2 Проверить установку

```bash
python -c "from ultralytics import YOLO; print('YOLO OK')"
```

Должно вывести: `YOLO OK`

---

## Шаг 3: Настроить config.py

Откройте файл `config.py` и измените параметры:

### 3.1 ОБЯЗАТЕЛЬНЫЕ параметры

```python
# IP вашей камеры (узнайте из приложения камеры)
CAMERA_URL = "rtsp://192.168.0.XXX:554/stream"  # ← ИЗМЕНИТЬ!

# IP ESP32 (из Serial Monitor)
ESP32_IP = "192.168.0.112"  # ← Проверить!
```

### 3.2 Опциональные параметры (можно оставить по умолчанию)

- `CONFIDENCE_THRESHOLD = 0.5` - уверенность детекции (0.3-0.7)
- `CENTER_THRESHOLD = 80` - зона центрирования (60-100)
- `MIN_DISTANCE = 100` - порог "кот далеко" (подбирается)
- `MAX_DISTANCE = 300` - порог "кот близко" (подбирается)

---

## Шаг 4: Бенчмарк моделей (опционально)

### Сравнение PyTorch vs OpenVINO

**Цель**: Узнать какая модель быстрее на вашем Intel i5-1235U

```bash
python benchmark_models.py
```

**Что происходит:**
1. Захватывает 50 кадров с камеры
2. Тестирует YOLO11 PyTorch (.pt)
3. Экспортирует модель в OpenVINO
4. Тестирует YOLO11 OpenVINO
5. Сравнивает FPS и время инференса

**Результат:**
```
📊 СРАВНЕНИЕ РЕЗУЛЬТАТОВ
Модель                    FPS            Время (мс)
----------------------------------------------------------
PyTorch (.pt)             8.5            117.6
OpenVINO                  15.2           65.8
----------------------------------------------------------
⚡ Ускорение OpenVINO:      1.79x
⏱️  Снижение времени:       44.0%

💡 РЕКОМЕНДАЦИЯ
✅ OpenVINO значительно быстрее!
   Обновите config.py:
   YOLO_MODEL = "yolo11n_openvino_model"
```

Если OpenVINO быстрее - обновите `config.py`:
```python
# Вместо:
YOLO_MODEL = "yolo11n.pt"

# Используйте путь к OpenVINO модели (из вывода бенчмарка):
YOLO_MODEL = "yolo11n_openvino_model"
```

---

## Шаг 5: Тестирование детекции

### Тест 1: Проверка детекции (БЕЗ моторов)

**Цель**: Убедиться что YOLO видит кота

```bash
python test_detection.py
```

**Что проверить:**
- ✅ Камера подключается
- ✅ Видео показывается в окне
- ✅ Кот обводится зелёным прямоугольником
- ✅ FPS достаточный (>5 fps)

**Если кот не детектируется:**
- Уменьшите `CONFIDENCE_THRESHOLD` в config.py (например, до 0.3)
- Проверьте освещение (YOLO лучше работает при хорошем свете)
- Попробуйте детектировать другие объекты (собака, человек)

**Выход**: Нажмите `q`

---

### Тест 2: Базовое следование (без компенсации дрейфа)

**Цель**: Проверить что робот следует за котом

1. Поставьте робота на пол
2. Отключите компенсацию дрейфа:

Откройте `config.py` и установите:
```python
DRIFT_COMPENSATION = 0.0  # Отключить компенсацию
```

3. Запустите:
```bash
python cat_follower_yolo11.py
```

**Что проверить:**
- ✅ Робот поворачивается к коту
- ✅ Едет вперёд когда кот в центре и далеко
- ✅ Останавливается когда кот близко
- ✅ Отъезжает назад если кот слишком близко

**Наблюдайте:**
- Насколько сильно робот уходит влево при движении вперёд
- Это нужно для калибровки компенсации

**Выход**: Нажмите `q` или `Ctrl+C`

---

### Тест 3: С компенсацией дрейфа

**Цель**: Настроить компенсацию ухода влево

1. Откройте `config.py`
2. Установите компенсацию:

```python
DRIFT_COMPENSATION = 0.15  # Начальное значение
```

3. Запустите:
```bash
python cat_follower_yolo11.py
```

**Калибровка:**

Если робот **всё равно уходит влево**:
```python
DRIFT_COMPENSATION = 0.20  # Увеличить
```

Если робот **уходит вправо**:
```python
DRIFT_COMPENSATION = 0.10  # Уменьшить
```

Если робот **едет прямо**:
```python
DRIFT_COMPENSATION = 0.0  # Отключить (не нужна компенсация)
```

**Подбирайте опытным путём!**

---

### Тест 4: Режим поиска

**Цель**: Проверить что робот патрулирует когда кот скрылся

1. Запустите робота
2. Положите кота за пределы кадра камеры
3. Наблюдайте поведение

**Ожидаемое:**
- Робот начинает медленно поворачиваться
- Двигается вперёд с паузами
- Меняет направление каждые 3 секунды
- Когда кот появляется - снова следует за ним

---

## Параметры калибровки

После базовых тестов откалибруйте параметры в `config.py`:

### Пороги расстояния

Зависят от размера вашего кота и разрешения камеры:

```python
MIN_DISTANCE = 100  # Если кот маленький → уменьшить до 80
MAX_DISTANCE = 300  # Если кот большой → увеличить до 350
```

**Как подобрать:**
1. Запустите `test_detection.py`
2. Поставьте кота на желаемое расстояние
3. Посмотрите "Size: XXXpx" на экране
4. Установите `MIN_DISTANCE` и `MAX_DISTANCE` вокруг этого значения

### Зона центрирования

```python
CENTER_THRESHOLD = 80  # Если робот дёргается → увеличить до 100
                       # Если не успевает центрироваться → уменьшить до 60
```

### Скорости

```python
SPEED_FORWARD_FAST = 180   # Макс скорость вперёд (150-255)
SPEED_TURN_FAST = 150      # Скорость поворота (100-200)
SPEED_SEARCH = 100         # Скорость поиска (80-120)
```

---

## Управление

### Горячие клавиши

- **q** - выход из программы
- **Ctrl+C** - аварийная остановка

### Команды ESP32 (для ручного тестирования)

Вы всегда можете управлять вручную через браузер:

```
http://192.168.0.112/forward   # Вперёд 2 сек
http://192.168.0.112/backward  # Назад 2 сек
http://192.168.0.112/left      # Влево 0.5 сек
http://192.168.0.112/right     # Вправо 0.5 сек
http://192.168.0.112/stop      # Стоп
```

---

## Решение проблем

### Проблема: Камера не подключается

**Ошибка**: `Cannot connect to camera`

**Решение**:
1. Проверьте RTSP URL в config.py
2. Проверьте что камера включена и доступна
3. Попробуйте открыть URL в VLC Player
4. Проверьте что камера и ноутбук в одной сети WiFi

### Проблема: Кот не детектируется

**Решение**:
1. Уменьшите `CONFIDENCE_THRESHOLD` до 0.3
2. Улучшите освещение
3. Проверьте что кот не спит/не прячется
4. Попробуйте детектировать себя (YOLO детектирует людей как class_id=0)

### Проблема: Низкий FPS (<3 fps)

**Решение**:
1. Используйте модель nano: `YOLO_MODEL = "yolo11n.pt"`
2. Уменьшите разрешение:
   ```python
   FRAME_WIDTH = 416
   FRAME_HEIGHT = 416
   ```
3. Закройте другие программы

### Проблема: Робот дёргается

**Решение**:
1. Увеличьте `CENTER_THRESHOLD` до 100
2. Увеличьте `COMMAND_INTERVAL` до 0.15
3. Уменьшите скорости в config.py

### Проблема: ESP32 не отвечает

**Ошибка**: `Connection error to ESP32`

**Решение**:
1. Проверьте что ESP32 подключён к WiFi (Serial Monitor)
2. Проверьте IP адрес в config.py
3. Пингуйте ESP32: `ping 192.168.0.112`
4. Перезагрузите ESP32

---

## Дополнительные возможности

### Отключить видео (для экономии ресурсов)

В `config.py`:
```python
SHOW_VIDEO = False
```

Робот будет работать без отображения окна.

### Логирование команд

В `config.py`:
```python
LOG_COMMANDS = True
LOG_FILE = "robot_commands.log"
```

Все команды будут записываться в файл.

### Отладка

В `config.py`:
```python
DEBUG = True  # Подробный вывод в консоль
SHOW_FPS = True  # Показывать FPS на видео
SHOW_CENTER_LINES = True  # Показывать линии центрирования
```

---

## Следующие шаги

После успешного тестирования можно:

1. **Добавить сглаживание** - усреднение последних N детекций
2. **PID-контроллер** - для более плавного движения
3. **Детекция препятствий** - остановка при столкновении
4. **Web-интерфейс** - управление через браузер
5. **Автономное питание** - установка PowerBank/батареи на робота

---

## Полезные команды

```bash
# Активировать окружение
cat_env\Scripts\activate

# Запуск тестового режима (только детекция)
python test_detection.py

# Запуск рабочего режима (следование за котом)
python cat_follower_yolo11.py

# Деактивировать окружение
deactivate
```

---

## Структура проекта

```
ESP32_car_cat/
├── config.py                    # ← НАСТРОЙТЕ ЗДЕСЬ ПАРАМЕТРЫ
├── cat_follower_yolo11.py       # Основной скрипт
├── test_detection.py            # Тест детекции
├── README_YOLO11.md             # Эта инструкция
├── CLAUDE.md                    # Документация проекта
├── esp32_cat_follower_simple/   # Arduino скетч
└── cat_env/                     # Виртуальное окружение Python
```

---

## Удачи! 🐱🤖

Если что-то не работает - проверьте все шаги ещё раз.
Калибровка может занять время - это нормально!
