# Инструкция по тестированию прошивки v2.1

## 🎯 Цель тестирования

Проверить исправление критического бага с обработкой очереди команд, который был обнаружен в v2.0.

---

## 🔧 Что исправлено в v2.1

### Критический баг v2.0 (строка 158):
```cpp
// БЫЛО (v2.0):
if (!waitingForDirectionChange && queueCount > 0 && !autoStopEnabled) {
    // ❌ Очередь НЕ обрабатывалась, пока autoStopEnabled == true
}

// СТАЛО (v2.1):
if (!waitingForDirectionChange && queueCount > 0 && currentState == STOPPED) {
    // ✅ Очередь обрабатывается после остановки мотора
}
```

**Проблема:** В v2.0 команды добавлялись в очередь, но **не выполнялись**, потому что `autoStopEnabled` был всегда `true` во время движения!

### Другие улучшения v2.1:
- ✅ Оптимизированы таймеры автостопа:
  - Forward/Backward: 2.0с → **1.5с** (-25%)
  - Left/Right: 0.5с → **0.4с** (-20%)
- ✅ Добавлено детальное логирование всех команд
- ✅ Добавлены счетчики для отслеживания:
  - `queuedCommands` - сколько команд было отложено
  - `commandIdCounter` - уникальный ID каждой команды
- ✅ Улучшен веб-интерфейс с эмодзи индикаторами

---

## 📋 План тестирования

### Этап 1: Загрузка прошивки v2.1

1. **Откройте Arduino IDE**
   ```
   Файл → Открыть
   D:\Projects\ESP32_car_cat\esp32_cat_follower_improved\esp32_cat_follower_improved.ino
   ```

2. **Проверьте настройки платы:**
   - Инструменты → Плата: **ESP32S3 Dev Module**
   - USB CDC On Boot: **Enabled** ⚠️ (ВАЖНО!)
   - PSRAM: **OPI PSRAM**
   - Flash Size: **8MB**
   - Partition Scheme: **8M with spiffs**
   - Upload Speed: **921600**

3. **Загрузите прошивку:**
   - Подключите ESP32 через USB-C порт "COM"
   - Выберите правильный COM-порт
   - Нажмите **Загрузить** (стрелка →)
   - Если не грузит: держите BOOT + нажмите RST + отпустите RST + отпустите BOOT

4. **Откройте Serial Monitor (115200 baud):**
   ```
   Инструменты → Монитор порта (или Ctrl+Shift+M)
   ```

5. **Ожидаемый вывод в Serial Monitor:**
   ```
   ========================================
   ESP32 Cat Follower Robot v2.1 - Starting...
   BUGFIX: Queue processing fixed!
   ========================================

   Connecting to WiFi: TP-Link_571B
   ...
   CONNECTED!
   IP address: 192.168.0.112
   ========================================
   Robot is ready!
   ```

   ⚠️ **ЗАПИШИТЕ IP-АДРЕС!** (например: 192.168.0.112)

---

### Этап 2: Ручное тестирование через браузер

**Цель:** Проверить базовую работоспособность и статистику.

1. **Откройте браузер:**
   ```
   http://192.168.0.112/
   ```

2. **Проверьте веб-интерфейс:**
   - ✅ Должен отображаться заголовок "ESP32 Cat Following Robot v2.1"
   - ✅ Должна быть надпись "BUGFIX: Queue processing fixed!"
   - ✅ Кнопки с временами: Forward (1.5s), Turn Left (0.4s), etc.

3. **Выполните тестовую последовательность:**
   ```
   1. Нажмите "Move Forward (1.5s)"
      → Подождите 1.5 сек (робот едет вперед)
      → Робот автоматически остановится

   2. Нажмите "Move Backward (1.5s)"
      → Подождите 1.5 сек (робот едет назад)
      → Робот автоматически остановится

   3. Обновите страницу (F5)

   4. Проверьте статистику:
      - Total commands: 2
      - Executed: 2
      - Queued: 0 (команды выполнялись сразу)
      - Dropped: 0 ✅
   ```

4. **Тест быстрой последовательности команд:**
   ```
   Быстро (с интервалом 0.5 сек) нажмите:
   1. Forward
   2. Left
   3. Forward
   4. Right
   5. Stop

   Обновите страницу и проверьте:
   - Total commands: 7 (2 предыдущих + 5 новых)
   - Queued: 3-4 (некоторые команды должны были попасть в очередь)
   - Dropped: 0 ✅ (главное - нет потерь!)
   ```

---

### Этап 3: Мониторинг Serial Monitor

**Цель:** Проверить детальное логирование команд.

**Пример ожидаемого вывода:**

```
[CMD #1] Received: FORWARD @ 150 | Current state: STOPPED | autoStop: OFF
▶️  Moving FORWARD @ 150 PWM (1.5 sec)

[CMD #2] Received: LEFT @ 150 | Current state: FORWARD | autoStop: ON
⏳ Motor busy, adding to queue...
📝 Command queued: LEFT @ 150 (queue: 1/5)

[CMD #3] Received: FORWARD @ 180 | Current state: FORWARD | autoStop: ON
⏳ Motor busy, adding to queue...
📝 Command queued: FORWARD @ 180 (queue: 2/5)

Auto-STOP (timer expired)

▶️  Executing queued command: LEFT @ 150
⬅️  Turning LEFT @ 150 PWM (0.4 sec)

Auto-STOP (timer expired)

▶️  Executing queued command: FORWARD @ 180
▶️  Moving FORWARD @ 180 PWM (1.5 sec)
```

**Что проверяем:**
- ✅ Каждая команда имеет уникальный ID `[CMD #N]`
- ✅ Команды добавляются в очередь при `autoStop: ON`
- ✅ Очередь **ВЫПОЛНЯЕТСЯ** после `Auto-STOP (timer expired)`
- ✅ Эмодзи индикаторы: ▶️ (forward), ⏪ (backward), ⬅️ (left), ➡️ (right)
- ✅ Нет сообщений `❌ Command queue FULL!`

---

### Этап 4: Автоматизированное тестирование с motor_test_suite.py

**Цель:** Воспроизвести Test #30 (complex maneuver) и проверить success rate.

1. **Активируйте виртуальное окружение:**
   ```bash
   cd D:\Projects\ESP32_car_cat
   cat_env\Scripts\activate
   ```

2. **Запустите тестовый набор:**
   ```bash
   python motor_test_suite.py
   ```

3. **Выберите режим:**
   ```
   Выберите режим тестирования:
   1. Полный набор тестов (30 тестов, ~15 минут)
   2. Быстрый тест (10 тестов, ~5 минут)
   3. Только критичные тесты (5 тестов, ~2 минуты)

   >>> 3  ← Выбираем быстрый тест критичных проблем
   ```

4. **Ожидаемые результаты v2.1:**

   | Тест | v2.0 (старая) | v2.1 (новая) | Статус |
   |------|---------------|--------------|--------|
   | Test #28 (forward→backward) | ❌ FAIL (1663ms) | ✅ PASS (80-120ms) | Исправлено |
   | Test #30 (complex maneuver) | ❌ FAIL (пропуск 1-й команды) | ✅ PASS | **Должно быть исправлено!** |
   | Success Rate | 86.7% | **95-100%** | Улучшение |
   | Dropped commands | Неизвестно | **0** | Без потерь |

5. **Проверьте Serial Monitor во время теста:**
   ```
   ========== STATISTICS v2.1 ==========
   Total commands received: 30
   ├─ Executed immediately: 15
   ├─ Queued for later: 15
   ├─ Dropped (queue full): 0  ← ✅ ДОЛЖНО БЫТЬ 0!
   └─ Currently in queue: 0

   📊 Drop rate: 0.00% | Queue rate: 50.00%

   Current motor state: STOPPED | autoStop: OFF
   =====================================
   ```

---

### Этап 5: Проверка endpoint /stats (JSON API)

**Цель:** Убедиться что API возвращает корректные данные.

1. **Откройте в браузере или curl:**
   ```bash
   curl http://192.168.0.112/stats
   ```

2. **Ожидаемый JSON:**
   ```json
   {
     "total_commands": 30,
     "executed": 30,
     "dropped": 0,
     "queue_size": 0,
     "current_state": "STOPPED",
     "waiting_for_change": false,
     "auto_stop_enabled": false
   }
   ```

3. **Что проверяем:**
   - ✅ `"dropped": 0` - нет потерянных команд
   - ✅ `"queue_size": 0` - очередь пуста (все выполнено)
   - ✅ `"executed" == "total_commands"` - все команды выполнены

---

## 🚨 Возможные проблемы и решения

### Проблема 1: Test #30 всё ещё FAIL

**Симптом:**
```
Test #30 (complex): FAILED
Notes: "All executed: n"
```

**Возможные причины:**
1. **Очередь всё ещё переполняется**
   - Проверьте Serial Monitor: есть ли сообщения `❌ Command queue FULL!`?
   - **Решение:** Увеличьте `COMMAND_QUEUE_SIZE` с 5 до 10 (строка 51)

2. **Таймеры автостопа слишком длинные**
   - Команды застревают в очереди слишком долго
   - **Решение:** Уменьшите таймеры:
     - Forward/Backward: 1500 → 1000 мс
     - Left/Right: 400 → 300 мс

3. **Интервал между командами Python скрипта слишком короткий**
   - **Решение:** В `config.py` увеличьте `COMMAND_INTERVAL` с 0.7 до 1.0 сек

### Проблема 2: Dropped commands > 0

**Симптом:**
```
Serial Monitor:
❌ Command queue FULL! Dropping: FORWARD @ 180
Dropped commands: 3
```

**Причина:** Очередь переполняется быстрее, чем успевает обрабатываться.

**Решения:**
1. Увеличьте размер очереди: `const int COMMAND_QUEUE_SIZE = 10;`
2. Уменьшите таймеры автостопа (команды будут выполняться быстрее)
3. Увеличьте интервал между командами в Python

### Проблема 3: "Писк" при первой команде всё ещё есть

**Возможные причины:**
1. **Электрический писк моторов** - это нормально при старте с высоким PWM
   - **Решение:** Используйте плавный старт (ramp-up PWM):
     ```cpp
     for (int i = 0; i <= motorSpeed; i += 10) {
       analogWrite(leftMotorPin1, i);
       delay(10);
     }
     ```

2. **Буззер на плате ESP32** (если есть)
   - **Решение:** Отключите буззер в setup():
     ```cpp
     pinMode(BUZZER_PIN, OUTPUT);
     digitalWrite(BUZZER_PIN, LOW);
     ```

### Проблема 4: Success rate < 95%

**Если результаты v2.1 ≈ v2.0 (86.7%):**

1. Проверьте что загрузили **правильную прошивку**:
   - Serial Monitor должен показывать **v2.1**, а не v2.0
   - Веб-интерфейс должен иметь надпись "BUGFIX: Queue processing fixed!"

2. Проверьте логи в Serial Monitor:
   - Есть ли строки с `📝 Command queued:`?
   - Есть ли строки с `▶️  Executing queued command:`?
   - Если НЕТ - очередь не работает правильно!

---

## ✅ Критерии успеха v2.1

### Обязательные (MUST):
- ✅ **Dropped commands: 0** (нет потерь команд)
- ✅ **Test #30 (complex maneuver): PASS** (все 5 команд выполнились)
- ✅ **Success rate ≥ 95%** (28+ тестов из 30)

### Желательные (SHOULD):
- ✅ Средняя латентность < 150 мс (было 233 мс в v2.0)
- ✅ Нет пиков латентности > 500 мс (было 1940 мс в v2.0)
- ✅ Serial Monitor показывает детальные логи всех команд
- ✅ Веб-статистика показывает корректные значения

### Дополнительные (NICE):
- ✅ Минимальный PWM ≤ 136 (было 136 в v2.0)
- ✅ Пауза 150мс при смене направления наблюдается
- ✅ Веб-интерфейс отображает эмодзи ✅/❌ для dropped commands

---

## 📊 Сравнение версий

| Метрика | v1.0 | v2.0 | v2.1 (ожидается) |
|---------|------|------|------------------|
| Success Rate | 88.9% | 86.7% | **95-100%** |
| Dropped commands | ? | 0? | **0** |
| Test #28 (fwd→bwd) | ❌ FAIL | ✅ PASS | ✅ PASS |
| Test #30 (complex) | ❌ FAIL | ❌ FAIL | **✅ PASS** |
| Латентность avg | 96ms | 233ms | **< 150ms** |
| Пики латентности | 1929ms | 1940ms | **< 500ms** |
| Очередь команд | Нет | Есть (баг) | **Исправлено** |

---

## 🎯 Следующие шаги после успешных тестов

1. **Если все тесты PASS:**
   - ✅ v2.1 можно использовать для основного проекта
   - Обновите `config.py` с оптимальными параметрами из тестов
   - Запустите Python скрипт для отслеживания кота: `python files/cat_follower_robot.py`

2. **Если Test #30 всё ещё FAIL:**
   - Создайте отчет с логами Serial Monitor
   - Приложите JSON файл с результатами тестов
   - Мы проанализируем и создадим v2.2 с дополнительными исправлениями

3. **Оптимизация производительности:**
   - Настройте оптимальные PWM значения на основе тестов
   - Отрегулируйте таймеры автостопа под ваш робот
   - Протестируйте с реальной камерой и детекцией кота

---

## 📝 Контрольный список тестирования

- [ ] Загрузил прошивку v2.1 в ESP32
- [ ] Serial Monitor показывает "v2.1 - Starting..."
- [ ] IP адрес записан: _______________
- [ ] Веб-интерфейс открывается и показывает v2.1
- [ ] Ручное тестирование через браузер: ✅ PASS
- [ ] Serial Monitor показывает детальные логи с эмодзи
- [ ] Автоматизированные тесты запущены
- [ ] Test #30 (complex maneuver): ✅ PASS
- [ ] Success rate ≥ 95%
- [ ] Dropped commands = 0
- [ ] Endpoint /stats возвращает корректный JSON
- [ ] Готов к интеграции с Python скриптом детекции кота

---

**Удачи в тестировании! 🚀**

Если возникнут вопросы или проблемы - записывайте логи Serial Monitor и результаты тестов для анализа.
