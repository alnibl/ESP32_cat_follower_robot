# Оптимизации для плавной стабильной работы

## Что изменено в config.py

### 1. Снижена уверенность детекции
```python
CONFIDENCE_THRESHOLD = 0.4  # было 0.5
```
**Почему:** Кот будет детектироваться чаще, меньше пропусков

---

### 2. Увеличен интервал команд
```python
COMMAND_INTERVAL = 0.3  # было 0.1
```
**Почему:**
- Робот принимает решения каждые 300мс (вместо 100мс)
- Более плавные движения, меньше рывков
- Учитывает задержку видео ~2 секунды

---

### 3. Добавлен пропуск кадров
```python
FRAME_SKIP = 2  # Обрабатывать каждый 2-й кадр
```
**Почему:**
- Обработка ~12 FPS вместо 24 FPS
- Меньше нагрузка на процессор
- Более стабильная работа
- Меньше "дёрганий" робота

---

## Как это работает вместе?

### Временная диаграмма:

```
Время:      0ms    100ms   200ms   300ms   400ms   500ms   600ms
Кадр:       #1      #2      #3      #4      #5      #6      #7
Обработка:  ✅      ❌      ✅      ❌      ✅      ❌      ✅
Команда:    ✅      -       -       ✅      -       -       ✅
```

**Результат:**
- Детекция: каждые 200мс (~5 FPS обработки)
- Команды роботу: каждые 300мс (~3 команды/сек)
- Плавное управление с учётом задержки видео

---

## Настройка под ваши нужды

### Если робот слишком медленный:
```python
COMMAND_INTERVAL = 0.2  # Быстрее реакция
FRAME_SKIP = 1          # Обрабатывать все кадры
```

### Если робот дёргается:
```python
COMMAND_INTERVAL = 0.4  # Ещё более плавно
FRAME_SKIP = 3          # Обрабатывать каждый 3-й кадр
```

### Если кот часто теряется:
```python
CONFIDENCE_THRESHOLD = 0.3  # Ещё ниже порог
```

---

## Учёт задержки видео

### Проблема:
RTSP камера имеет задержку ~2 секунды между реальностью и видео на экране.

### Решение:
1. **Не спешить** - COMMAND_INTERVAL = 0.3 секунды
2. **Сглаживать** - пропуск кадров уменьшает резкие движения
3. **Робот не в реальном времени** - это нормально для вашей задачи

### Пример:
```
Реальность:       Кот движется вправо
Через 2 сек:      Видео показывает кота справа
Робот:            Плавно поворачивает вправо
Через 0.5 сек:    Робот продолжает поворот (если кот всё ещё справа)
```

Задержка компенсируется тем что:
- Кот обычно не двигается быстро
- Робот реагирует плавно и предсказуемо
- Каждые 300мс проверяется положение кота

---

## Преимущества плавной работы

✅ Меньше износ моторов (не резкие старты/стопы)
✅ Стабильнее детекция (не теряем кота при рывках)
✅ Предсказуемое поведение
✅ Меньше нагрузка на процессор
✅ Лучше для кота (не пугается резких движений) 🐱

---

## Модель побольше (опционально)

Если хотите лучше детектировать кота, можно попробовать:

```python
# В config.py:
YOLO_MODEL = "yolo11s.pt"  # Small вместо Nano
```

Затем заново экспортировать в OpenVINO:
```bash
python benchmark_models.py
```

**Small модель:**
- Лучше детекция (меньше пропусков)
- Медленнее (~15-18 FPS вместо 23 FPS)
- Но всё равно достаточно для робота

**Рекомендация:** Сначала протестируйте с Nano. Если кот слишком часто теряется - попробуйте Small.
