# =============================================================================
# ШАБЛОН КОНФИГУРАЦИИ - .env.example
# Configuration Template
# =============================================================================
# Скопируйте этот файл в .env и заполните своими данными
# Copy this file to .env and fill in your data
#
# Команда для копирования / Copy command:
#   Windows (PowerShell): Copy-Item .env.example .env
#   Windows (CMD):        copy .env.example .env
#   Linux/macOS:          cp .env.example .env
# =============================================================================

# ========== WiFi ПАРАМЕТРЫ ==========
# WiFi Parameters

# Имя WiFi сети (SSID)
# WiFi network name
WIFI_SSID=YOUR_WIFI_SSID

# Пароль WiFi сети
# WiFi password
WIFI_PASSWORD=YOUR_WIFI_PASSWORD

# ========== IP КАМЕРА ==========
# IP Camera

# RTSP URL камеры (включая логин/пароль если требуется)
# Camera RTSP URL (including login/password if required)
#
# Примеры форматов / Format examples:
#   - rtsp://192.168.0.100:554/stream
#   - rtsp://admin:password@192.168.0.100:554/stream
#   - rtsp://192.168.0.100:8554/live/stream1
#
# Замените на URL вашей камеры
# Replace with your camera URL
CAMERA_URL=rtsp://192.168.0.100:554/stream

# ========== ESP32 ==========
# ESP32 Controller

# IP адрес ESP32 в локальной сети
# ESP32 IP address in local network
#
# Этот адрес показывается в Serial Monitor при загрузке скетча
# This address is shown in Serial Monitor when sketch starts
#
# Примеры / Examples:
#   ESP32_IP=192.168.0.100
#   ESP32_IP=192.168.1.50
#
# Замените на IP адрес вашего ESP32
# Replace with your ESP32 IP address
ESP32_IP=192.168.0.100

# =============================================================================
# ИНСТРУКЦИЯ ПО НАСТРОЙКЕ
# Setup Instructions
# =============================================================================
#
# 1. Скопируйте этот файл:
#    Copy this file:
#      Copy-Item .env.example .env    (Windows PowerShell)
#      copy .env.example .env         (Windows CMD)
#      cp .env.example .env           (Linux/macOS)
#
# 2. Откройте .env в текстовом редакторе
#    Open .env in text editor
#
# 3. Заполните ваши данные:
#    Fill in your data:
#      - WIFI_SSID: имя вашей WiFi сети (только 2.4GHz!)
#      - WIFI_PASSWORD: пароль WiFi
#      - CAMERA_URL: RTSP URL вашей IP камеры
#      - ESP32_IP: IP адрес ESP32 (из Serial Monitor)
#
# 4. Сохраните .env файл
#    Save .env file
#
# 5. ВАЖНО: НЕ коммитьте .env в Git!
#    IMPORTANT: DO NOT commit .env to Git!
#    Файл .env уже добавлен в .gitignore
#    The .env file is already added to .gitignore
#
# =============================================================================
# ПРИМЕЧАНИЯ
# Notes
# =============================================================================
#
# - ESP32-S3 поддерживает только WiFi 2.4GHz (не 5GHz)
#   ESP32-S3 supports only 2.4GHz WiFi (not 5GHz)
#
# - Все устройства (ESP32, камера, PC) должны быть в одной локальной сети
#   All devices (ESP32, camera, PC) must be on the same local network
#
# - IP адрес ESP32 показывается в Serial Monitor (115200 baud) при запуске
#   ESP32 IP address is shown in Serial Monitor (115200 baud) at startup
#
# - Для Arduino скетчей (.ino) скопируйте WIFI_SSID и WIFI_PASSWORD в код вручную
#   For Arduino sketches (.ino) copy WIFI_SSID and WIFI_PASSWORD to code manually
#
# =============================================================================
