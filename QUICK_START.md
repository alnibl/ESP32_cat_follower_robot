# Быстрый старт - Робот следящий за котом

## Шаг 1: Настройка Arduino IDE

1. Следуйте инструкциям в файле `ARDUINO_IDE_SETUP.md`
2. Основные моменты:
   - Установите Arduino IDE 2.x
   - Добавьте URL для ESP32: `https://espressif.github.io/arduino-esp32/package_esp32_index.json`
   - Установите платформу ESP32 через Board Manager
   - Выберите плату: **ESP32S3 Dev Module**
   - Настройте параметры платы (особенно PSRAM и Flash Size)

## Шаг 2: Первый тест (только WiFi, без моторов)

### 2.1 Подготовка кода
1. Откройте файл `test_wifi_only/test_wifi_only.ino`
2. Измените WiFi данные:
   ```cpp
   const char* ssid = "ваша_WiFi_сеть";
   const char* password = "ваш_пароль";
   ```

### 2.2 Подключение платы
1. Подключите ESP32-S3 к компьютеру через USB-C кабель
2. Выберите правильный COM порт в Arduino IDE: **Tools → Port**
3. Если порт не отображается - установите драйвера CP210x

### 2.3 Загрузка кода
1. Нажмите кнопку **Upload** (стрелка вправо)
2. Дождитесь "Connecting..."
3. Если не подключается:
   - Нажмите и держите кнопку **BOOT** на плате
   - Нажмите кнопку **RST**
   - Отпустите **RST**, затем **BOOT**
   - Быстро нажмите **Upload** снова

### 2.4 Проверка результата
1. Откройте Serial Monitor: **Tools → Serial Monitor** (Ctrl+Shift+M)
2. Установите скорость: **115200 baud**
3. Вы должны увидеть:
   ```
   ========================================
   ESP32-S3 WiFi Test
   ========================================
   Connecting to: ваша_сеть
   ....
   SUCCESSFULLY CONNECTED TO WiFi!
   IP Address: 192.168.X.XXX
   ```
4. **ЗАПИШИТЕ ЭТОТ IP АДРЕС!** Он понадобится позже.

### 2.5 Тест через браузер
1. Откройте браузер на компьютере (он должен быть в той же WiFi сети)
2. Перейдите на адрес: `http://IP_АДРЕС_ESP32`
3. Вы должны увидеть веб-страницу с информацией о подключении
4. Попробуйте кнопки управления LED

**✅ Если всё работает - переходите к шагу 3!**

## Шаг 3: Подключение моторов

### 3.1 Схема подключения
См. файл `WIRING_DIAGRAM.md` для детальной схемы.

Краткая схема:
```
ESP32 GPIO1  → MX1508 IN1 (Левый мотор вперед)
ESP32 GPIO2  → MX1508 IN2 (Левый мотор назад)
ESP32 GPIO42 → MX1508 IN3 (Правый мотор вперед)
ESP32 GPIO41 → MX1508 IN4 (Правый мотор назад)
ESP32 GND    → MX1508 GND
```

Питание:
```
PowerBank 5V → MX1508 VCC, VM
PowerBank 5V → ESP32 (через USB или VIN)
PowerBank 5V → IP Camera
Общий GND для всех!
```

### 3.2 ВАЖНО перед подключением!
- ✅ Отключите всё от питания
- ✅ Проверьте, что все GND соединены вместе
- ✅ Проверьте полярность питания
- ✅ Убедитесь, что нет коротких замыканий

### 3.3 Загрузка кода управления моторами
1. Откройте `esp32_cat_follower_simple/esp32_cat_follower_simple.ino`
2. WiFi данные уже должны быть настроены (те же что в тесте)
3. Загрузите код на ESP32
4. Откройте Serial Monitor и запишите IP адрес

### 3.4 Тест моторов через браузер
1. Откройте `http://IP_АДРЕС_ESP32/` в браузере
2. Попробуйте команды:
   - Move Forward
   - Move Backward
   - Turn Left
   - Turn Right
   - Stop

**⚠️ Внимание:**
- Поднимите машинку так, чтобы колёса не касались пола
- Если мотор крутится не в ту сторону, поменяйте провода мотора местами

**✅ Если моторы работают - переходите к шагу 4!**

## Шаг 4: Настройка камеры

### 4.1 Подключение камеры к WiFi
1. Используйте приложение вашей IP камеры
2. Подключите камеру к той же WiFi сети что ESP32 и компьютер
3. Найдите RTSP URL камеры (обычно в настройках)
   - Примеры:
     - `rtsp://192.168.1.100:554/stream`
     - `rtsp://admin:password@192.168.1.100:554/stream1`

### 4.2 Тест RTSP потока
1. Скачайте VLC Media Player (если нет)
2. Откройте VLC: **Media → Open Network Stream**
3. Введите RTSP URL камеры
4. Нажмите **Play**
5. Вы должны увидеть видео с камеры

**✅ Если видео работает - переходите к шагу 5!**

## Шаг 5: Python скрипт для детекции кота

### 5.1 Установка Python зависимостей
Откройте терминал (PowerShell или CMD) и выполните:

```bash
# Создание виртуального окружения
python -m venv cat_env

# Активация (Windows)
cat_env\Scripts\activate

# Установка библиотек
pip install opencv-python opencv-contrib-python numpy requests
```

### 5.2 Настройка Python скрипта
1. Откройте файл `files/cat_follower_robot.py`
2. В конце файла измените параметры:
   ```python
   CAMERA_URL = "rtsp://ваш_RTSP_URL"
   ESP32_IP = "IP_адрес_ESP32"
   ```

### 5.3 Тест Python скрипта
```bash
# Убедитесь что виртуальное окружение активировано
python files/cat_follower_robot.py
```

Вы должны увидеть:
- Окно с видео с камеры
- Если кот детектируется - зелёный прямоугольник вокруг него
- В консоли - команды отправляемые на ESP32

**Нажмите 'q' для выхода**

## Шаг 6: Финальная сборка

1. Закрепите все компоненты на платформе:
   - ESP32 в защищённом месте
   - MX1508 драйверы рядом с моторами
   - IP камера спереди платформы
   - PowerBank по центру для баланса

2. Проверьте все соединения
3. Включите питание
4. Запустите Python скрипт
5. Посадите кота перед роботом 😺

## Решение проблем

### WiFi не подключается
- Проверьте правильность SSID и пароля
- Убедитесь что роутер работает на 2.4GHz (ESP32 не поддерживает 5GHz)
- Попробуйте переместить ESP32 ближе к роутеру

### Моторы не работают
- Проверьте подключение проводов
- Убедитесь что драйверы получают питание (светодиод на MX1508)
- Проверьте что PowerBank выдаёт достаточно тока

### Камера не подключается
- Проверьте RTSP URL в VLC
- Убедитесь что камера и компьютер в одной сети
- Попробуйте пинговать IP камеры: `ping IP_КАМЕРЫ`

### Python скрипт не находит кота
- Используйте хорошее освещение
- Попробуйте использовать YOLO версию скрипта
- Настройте параметры детекции в коде

## Дополнительные улучшения

После базовой работы можно:
- Использовать YOLO для лучшей детекции
- Добавить автономное питание (батареи 18650)
- Улучшить алгоритм следования
- Добавить датчики расстояния
- Записывать видео с камеры

## Полезные ссылки

- Arduino ESP32 документация: https://docs.espressif.com/projects/arduino-esp32/
- OpenCV Python туториалы: https://docs.opencv.org/4.x/d6/d00/tutorial_py_root.html
- YOLO для детекции объектов: https://github.com/AlexeyAB/darknet

---

**Удачи с проектом! 🚗😺**
